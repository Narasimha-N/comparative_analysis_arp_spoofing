The shell scripts present in this directory will be used to generate the benign datasets on respective victim hosts from victim1 to victim5.

The txt file named "mininet_environment_commands.txt" file is configuration of all hosts that include five victims, one attacker and one switch.